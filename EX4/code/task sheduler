#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#define MAX(a, b) ((a) > (b) ? (a) : (b))
int leastInterval(char* tasks, int tasksSize, int n) {
        int freq[26] = {0};
    for (int i = 0; i < tasksSize; i++) {
        freq[tasks[i] - 'A']++;
    }
    int max_freq = 0;
    for (int i = 0; i < 26; i++) {
        if (freq[i] > max_freq) {
            max_freq = freq[i];
        }
    }
    int max_idle_slots = (max_freq - 1) * n;

    for (int i = 0; i < 26; i++) {
        if (freq[i] == max_freq) {
        }
    }
    int total_max_freq_tasks = 0;
    for (int i = 0; i < 26; i++) {
        if (freq[i] == max_freq) {
            total_max_freq_tasks++;
        }
    }    int minimum_intervals = (max_freq - 1) * (n + 1) + total_max_freq_tasks;
    return MAX(tasksSize, minimum_intervals);
}
